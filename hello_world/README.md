# Standard Datastar Hello World example

This directory contains an example Hello World app

You can build and run the app using

```
zig build run
```

Then open your webbrowser to http://localhost:8080
