# Example kitchen sink app for different Zig webservers

This `examples` directory contains a kitchen sink app that exersizes all of Datastar, with implementations
and build scripts for a variety of different Zig webservers.

```
zig build stdlib
zig build dusty
zig build httpz
```

Will build the kitchen sink example, using Zig stdlib, Dusty, or http.zig respectively


